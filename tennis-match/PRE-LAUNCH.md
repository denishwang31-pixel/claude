# 정식 출시 전 정리 목록

> **이 문서의 용도**
> 지금 우리 클럽에서 쓰는 데는 문제가 없지만, **낯선 사람이 스토어에서
> 받아 쓰기 시작하면 곤란해지는 것들**을 모아 둔다.
>
> "나중에 하자"는 말은 반드시 잊힌다. 그래서 기능을 하나 추가할 때마다
> 그 기능이 남긴 빚도 여기에 같이 적는다.
>
> - 상태는 `[ ]` 미처리 / `[x]` 처리됨 으로만 표시한다.
> - 처리된 항목은 지우지 않는다. 지우면 "이거 했었나?"를 다시 조사하게 된다.
> - 마지막 갱신: 2026-08-21 (B 항목 정리 — 개발 잔재·인수인계)

---

## A. 이게 안 되면 출시 자체가 막힌다

- [ ] **약관·개인정보처리방침 — 본문은 끝났다. 웹 게시만 남았다**
      (2026-08-21) `src/lib/legalText.js`. 앱에서는 [더보기] → [약관·개인정보].
      빈칸 0개. 이 앱이 **실제로** 무엇을 모으는지 코드를 보고 적었다 —
      견본을 베끼면 모으지도 않는 항목이 적힌다.

      **남은 일: 웹에 올리고 그 주소를 플레이 콘솔에 넣기**
        구글 플레이 콘솔이 **공개 URL** 을 요구한다. 앱 안 화면은 그 요구를
        대신하지 못한다. GitHub Pages, Notion 공개 페이지, Google Sites 등
        무료로 충분하다. 올릴 때는 `legalText.js` 의 본문을 그대로 붙여 넣는다.
        ⚠️ 앱과 웹의 내용이 다르면 그게 더 문제가 된다. 고칠 때 양쪽 다 고친다.

      **기능을 추가하면 여기도 고쳐야 한다**
        수집 항목이 늘었는데 방침이 그대로면 실제와 달라진다. 그 상태가
        발각되면 경고 후 앱이 내려간다. 특히 결제·배송지를 붙일 때.

- [ ] **Firebase 보호는 규칙이 하고 있다 — App Check 를 붙일지 정한다**
      (2026-08-19 정정) `firebaseConfig.js` 에는 이미 **실제 키**가 들어 있다.
      자리표시자가 아니다.

      그리고 이건 생각보다 급하지 않다. Firebase **웹 API 키는 비밀이 아니다** —
      설계상 클라이언트에 공개되는 값이고, 앱을 뜯으면 어차피 보인다. 실제
      방어선은 보안 규칙이고, 그건 239건의 테스트로 지키고 있다.

      그래도 남는 것: 키만 있으면 **우리 앱이 아닌 스크립트로도** 요청을 보낼 수
      있다. 규칙이 막아 주지만 읽기 비용은 나간다. 그걸 막는 것이 App Check다.
      사람이 늘고 비용이 눈에 띄면 그때 붙인다. 지금 급한 일은 아니다.

- [ ] **전화번호 인증 방식 결정 (R-1)** — 지금은 Firebase JS SDK 골격이고
      `expo-firebase-recaptcha` 는 이미 deprecated 다. 실사용 인증을
      본격적으로 열려면 `@react-native-firebase/auth` + dev build 로
      갈아타야 한다. 사람이 늘기 전에 정해야 데이터 이관이 없다.

- [ ] **`expo-updates` — 설정은 넣었고 빌드 1회가 남았다** (2026-08-18 착수)
      패키지·`runtimeVersion`(appVersion 정책)·채널(development/preview/
      production)까지 넣었다. 남은 것은 `eas update:configure` 한 번과
      **빌드 1회**다. 네이티브 모듈이라 OTA 로는 스스로를 넣을 수 없다.

      그 빌드가 끝나면 JS 변경은 `eas update --branch preview` 로 몇 초 만에
      나간다. 최근 작업(참석 투표·교류전·대진표 표시)은 전부 자바스크립트였고,
      2026-08 에 EAS 안드로이드 무료 한도(월 15회)가 떨어져 13일 밀린 것도
      이것만 있었으면 겪지 않았다.

      **주의**: `runtimeVersion` 이 `version`(0.1.0)을 따른다. 네이티브가
      바뀌면 `version` 을 올려야 옛 빌드가 못 받는 JS 를 받지 않는다.
      네이티브를 그대로 두고 `version` 만 올리면 기존 사용자가 업데이트를
      못 받으므로, 이 값은 함부로 건드리지 않는다.

- [ ] **스토어 스크린샷 · 소개문구** — 미착수.
      아이콘·스플래시 이미지 파일은 `assets/` 에 있고 app.json 이 가리키고
      있다(icon / adaptive-icon / splash / notification-icon). 다만 만든 지
      오래됐으니 출시 전에 한 번은 실제로 띄워 보고 판단한다.
      스크린샷과 소개문구는 아직 아무것도 없다 — 이게 없으면 등록이 안 된다.

- [ ] **버전 체계** — `app.json` 이 `0.1.0` / `versionCode: 1` 그대로다.
      출시 전에 1.0.0 으로 올리고, 이후 빌드마다 versionCode 를 올리는
      규칙을 정해 둔다(안 올리면 업로드가 거부된다).

---

## B. 지금은 편해서 켜 둔 것 — 남 앞에 내보내기 전에 꺼야 한다

- [x] **온보딩 다시 열기 — 다시 보니 지울 것이 아니었다** (2026-08-21 재검토)
      코드를 따라가 보니 `resetOnboarding` 은 **클럽이 없는 사람의 화면에서만**
      쓰인다. 그 사람에게는 들어갈 길이 있어야 하므로 정상이다.
      클럽이 있는 사람에게 보이는 [새 클럽 만들기]·[다른 클럽 찾기]는
      개발 잔재가 아니라 실제 기능이고, 이미 `ghost` 로 낮게 그려져 있다.

      "정리한다"고 적어 뒀던 판단을 취소한다. 멀쩡한 기능을 지울 뻔했다.

- [ ] **오프라인 등록 회원(`local:` 아이디)** — 총무가 손으로 추가한
      회원은 로그인할 수 없는 유령 계정이다. 지금은 대진·회비 계산에는
      들어가는데 본인은 앱을 못 쓴다. 나중에 그 사람이 실제로 가입했을 때
      **기존 기록과 합치는 방법**이 없다. 합치기 기능이 필요하다.

- [ ] **카카오 지도 키 없음** — `keys.js` 가 빈 문자열이라 코트 지도가
      간이 모드로만 뜬다. 기능 저하일 뿐 죽지는 않지만, 코트 검색이
      이 앱의 유입 경로라면 출시 전에 넣는 것이 낫다.

---

## C. 사람이 늘면 조용히 터지는 것

**2026-08-21 다섯 항목 모두 처리. 자세한 내용은 E 로 옮겼다.**

여기 있던 것들의 공통점은 "고장 나도 오류가 안 뜬다"였다. 그래서
고치면서 검사도 같이 넣었다(`scripts/test-scale.mjs`, 63건).

- [x] 모임 구독에 기간 제한이 없다 → 최근 12개월 창 + 필요할 때 과거 조회
- [x] 채팅을 300개 받아 앱에서 거른다 → 채널별 서버 필터 + 복합 색인
- [x] 교류전 초대에 만료가 없다 → 지난 것을 아래로 접는다(지우지 않는다)
- [x] 교류전 명단에 상대 클럽 이름이 남는다 → 보관 기간 1년 + 지우는 길
- [x] 코트 데이터가 손으로 만든 눈금이다 → 받은 날짜 표시 + 이용자 신고

---

## D. 외부 계약·비용이 걸린 것 (돈과 시간이 필요하다)

- [ ] **⭐ 대회 중계·운영 수익 (2026-08-21 설계, 2026-08-21 정정)**

      ⚠️ **처음에 잘못 잡았다.** "대회를 일정에 녹인다"고 적으면서
         `clubs/{id}/tournaments` — 우리 클럽이 여는 월례대회 —를 일정에
         합쳤다. 그런데 수익이 나오는 대회는 그게 아니라 협회·지자체·
         기업이 여는 큰 대회(KTA, 시도협회, 던롭 X-OPEN, FILA Club Match)다.
         둘은 이름만 같지 완전히 다른 물건이다.

      **그래서 갈라 놓았다 — 겹치면 안 된다**

        클럽 대회  `clubs/{id}/tournaments` · src/lib/agenda.js
          우리끼리 여는 것. 참가자도 대진도 우리가 갖는다. 일정에 뜬다.

        공개 대회  `openTournaments`(루트) · src/lib/openTournament.js
          밖에서 열리는 것. 우리는 "이런 게 있다"만 알려 준다. 신청은
          주최 측 사이트에서. [찾아보기] → [대회 찾기] 화면.

      섞으면 안 되는 이유 세 가지 (openTournament.js 머리말에도 적었다)
        1. 성격이 다르다. 클럽 일정은 "내가 가야 하는 것", 공개 대회는
           "관심 있으면 나가는 것". 한 줄에 섞으면 화요일 정기 모임과
           전국대회가 같은 무게로 보인다.
        2. 양이 다르다. 전국 대회는 한 달에 수십 건, 우리 일정은 여덟 번.
           섞으면 우리 일정이 묻힌다.
        3. 달력이 무의미해진다. 거의 모든 날에 점이 찍혀서 "이번 주에
           우리 뭐 있지"를 볼 수 없게 된다.

      일정 화면에 남긴 것은 **[협회·오픈 대회 찾아보기] 한 줄뿐**이다.
      목록에도 달력에도 안 들어간다. 그 줄에서 openTournaments 를
      구독하지 않는 것도 일부러다 — 일정은 모든 회원이 매일 여는
      화면이라, 루트 컬렉션 구독을 하나 더 붙이면 읽기 비용이 사람
      수만큼 곱해진다. 건수는 넘어간 화면에서 센다.

      `scripts/test-opens.mjs` 가 "섞이지 않는가"를 직접 검사한다.
      KIND 에 'open' 을 넣거나 라벨을 "클럽 대회"에서 "대회"로 되돌리면
      실패한다(일부러 되돌려 두 건이 깨지는 것을 확인했다).

      **아직 안 넣은 것 (돈이 걸린 부분)**
        1. **중계 화면** — 대회 기간 중(`OPEN_STATE.LIVE`) 경기 점수가
           실시간으로 갱신되는 한 장. 클럽 밖 사람도 링크로 볼 수 있어야
           의미가 있다(그래야 스폰서가 붙는다). 다만 공개 대회는 대진을
           우리가 갖고 있지 않으므로, 주최 측과 자료를 받는 합의가
           선행되어야 한다 — 여기가 진짜 관문이다.
        2. **대회 스폰서 배너** — 첨부 사진의 FILA Club Match 자리.
           `AD_SLOTS` 에 대회 전용 슬롯을 하나 더 두고 [대회 찾기]와
           중계 화면 상단에만 띄운다. **홈·일정에는 안 띄운다** — 매일
           쓰는 화면이 광고판이 되면 앱을 안 쓰게 된다.
        3. **대회 운영 대행 수수료** — 참가비 수납·대진 운영을 대신 맡는
           경우. 참가비가 앱을 거치면 PG 계약과 정산 의무가 생긴다.
           초기에는 계좌 안내로 충분하고 규모가 커지면 붙인다.

      순서: 중계(1) → 스폰서(2) → 수수료(3).
      (1)이 있어야 사람이 모이고, 사람이 모여야 (2)가 팔리고,
      (2)가 팔려야 (3)을 이야기할 수 있다. 바꾸면 아무것도 안 팔린다.

      당장 할 수 있는 것: 앱 운영자가 [대회 찾기]에서 요강을 손으로
      옮겨 적는다. 협회 사이트를 긁어 오는 것은 사이트마다 형식이 달라
      코트 데이터와 같은 문제(조용히 죽는 링크)를 만든다. 손으로 넣는
      대신 건수를 적게 유지하고, 접수 마감일만은 반드시 채운다 —
      그 날짜 하나가 이 화면의 존재 이유다.

- [ ] **카카오 알림톡** — 앱을 안 까는 회원(특히 40~60대)이 이 앱의 가장 큰
      구멍이다. 정답이지만 카카오 비즈메시지 채널 개설 + 발신프로필 심사가
      먼저다. 건당 8~9원, 회원 30명 × 월 4건이면 월 1,080원 수준.

- [ ] **고정형 가상계좌** — 입금자명 문제가 원천 소멸한다. 단 PG 계약이
      필요하고, 앱이 돈을 직접 보관하면 전자금융거래법(선불전자지급수단)에
      걸리므로 **PG사 명의 계좌 + 정산 대행** 형태로만 가능하다.
      통신판매중개업 신고도 따라온다.

- [ ] **영수증 OCR** — 지출 등록 자동화. 외부 OCR 연동 필요.

- [ ] **스토어 비용** — Google Play $25(1회), Apple Developer $99(연).

- [ ] **Blaze 요금제** — Cloud Functions 를 쓰는 순간 필수다. 무료 쿼터
      안에서 돌지만 카드 등록은 되어 있어야 한다.

- [ ] **소셜 로그인 (카카오 · 구글 · 네이버)** — 앱 쪽 판단 로직은 넣었다
      (`src/lib/social.js`). 지금은 키가 없어 버튼이 **안 그려진다**.
      눌러도 안 되는 버튼을 두면 앱이 고장 난 줄 알기 때문이다.

      받아야 하는 것
        카카오  developers.kakao.com → 앱 생성 → Android 플랫폼 등록
                (패키지명 + 키 해시) → 카카오 로그인 켜기 → Redirect URI
                → REST API 키 · 네이티브 앱 키
        구글    Firebase 콘솔 → Authentication → Google 사용 설정
                → Google Cloud 에서 Android OAuth 클라이언트(SHA-1 지문)
                → 웹 클라이언트 ID · 안드로이드 클라이언트 ID
        네이버  developers.naver.com → 애플리케이션 등록 → 네이버 로그인
                사용 → Android 패키지명 → Client ID · Secret

      코드로 해야 하는 것 (키가 온 뒤)
        · `expo-auth-session` + `expo-web-browser` 추가
        · 카카오·네이버는 Firebase 가 모르는 제공자다. 받은 토큰을 검증해
          Firebase 커스텀 토큰으로 바꿔 주는 Cloud Function 이 하나 필요하다.
          구글은 Firebase 기본 제공자라 서버가 필요 없다.
        · `SOCIAL_CONFIG` 에 키를 넣는다. 코드에 직접 적지 말고 app.json 의
          extra 나 EAS 시크릿에서 읽어 넣는다.

      ⚠️ **APK 를 새로 빌드해야 한다.** 네이티브 모듈이 들어가므로
      `eas update` (OTA) 로는 안 나간다. `needsNativeRebuild()` 가 이걸
      코드에서도 알려 준다.

- [ ] **앱 안에서 카드 결제 (PG 계약)** — 회비를 "납부" 누르면 카드로 결제되고
      자동으로 납부 처리되는 것. 사업자등록증 · 정산계좌 · PG 심사가 필요하고
      건당 수수료(대략 2~3%)가 붙는다.

      **지금은 대신 이렇게 동작한다** (계약 없이 이미 됨)
        [내 회비] → 미납 달의 [납부하기] → 토스/카카오페이/네이버페이 중 선택
        → 송금 앱이 계좌·금액이 채워진 채 열린다 → 회원은 확인만 누른다.
        입금되면 총무가 [입금 대사]에서 확인해 납부로 바꾼다.
        수수료 0원이고, 클럽 규모에서는 이쪽이 오히려 낫다.

      켜는 법: [클럽 설정] → 회비 받을 계좌 에 은행·계좌번호를 넣는다.
      카카오페이·네이버페이는 총무가 자기 앱에서 만든 송금 링크를 붙여
      넣으면 그 버튼도 생긴다(개인 송금 딥링크는 공개된 방식이 없다).

      ⚠️ **실기기에서 한 번은 확인해야 한다.** 토스 송금 주소
      (`supertoss://send?...`)는 토스가 문서로 공개한 규격이 아니라 널리
      쓰이는 방식이다. 안 열리면 앱이 계좌번호를 복사해 주도록 해 두었으니
      회원이 막다른 길에 갇히지는 않는다.

- [ ] **용품 드랍십 (앱에서 직접 주문받기)** — 상품 등록 화면에 [드랍십] 방식과
      공급가·마진 계산은 넣었다(`src/lib/dropship.js`). 주문 상태 흐름과
      보안 규칙도 만들어 두었다. 하지만 **실제로 주문을 받으면 안 된다** —
      아래가 먼저다.

        · 사업자등록 (통신판매업 신고 포함)
        · 결제 수단 — 위의 PG 계약과 같은 문제다
        · 교환·반품 규정 고지 (전자상거래법)
        · 공급처와의 발주 계약 (재고·배송·반품 책임 범위)

      **초기에는 링크형만 쓰는 것이 맞다.** 판매처로 보내기만 하면 앱은
      "판매자"가 아니라서 위 의무가 생기지 않는다. 제휴에 가입하면
      링크에 코드만 붙여 수수료를 받을 수 있다(`src/lib/ads.js` 의
      `PARTNERS` 에 코드만 채우면 화면은 그대로).

- [ ] **⭐ 코치 광고 월 구독 결제 (이 앱의 수익모델)** — 나중에 만든다. 기록만.

      **돈의 방향을 다시 못박아 둔다: 코치가 앱에 낸다.**
      코치는 영상 노출로 유튜브 구독자와 레슨 문의를 얻고, 그 대가로 월
      광고비(3~5만원)를 앱에 낸다. (2026-08-19 이전 메모에는 반대로
      적혀 있었다 — 그때는 앱이 코치에게 주는 것으로 잘못 알았다)

      **지금 되는 것**: [코치 승인·광고비] 화면에서 청구서를 만들고, 입금을
      손으로 확인해 표시한다. 청구·입금·미수금 합계가 나온다. 광고가 나갔는데
      청구서를 안 만든 코치는 경고로 잡아 준다(그대로 두면 못 받는 돈이다).
      무료 체험·초기 입점 혜택은 [면제]로 둔다.

      **나중에 만들 것 — 자동 구독 결제**
        · 코치가 카드를 등록하면 매월 자동으로 결제되는 정기 구독
        · 결제 실패 / 미납이 며칠 지나면 광고(영상)를 자동으로 내릴지
          — 정책을 먼저 정해야 한다. 지금은 앱 주인이 손으로 내린다.
        · 세금계산서·현금영수증 발행 (코치는 대개 사업자다)
        · 코치가 자기 청구·결제 내역을 보는 화면 (지금은 앱 주인만 본다)

      **필요한 것**
        · 사업자등록 + PG 계약 (정기결제/빌링키 지원 상품이어야 한다.
          일반 결제만 되는 상품은 매달 코치가 직접 결제해야 해서 의미가 없다)
        · 구독 결제는 앱스토어 정책도 본다. **디지털 콘텐츠 구독이면 인앱결제를
          강제당할 수 있고 수수료가 15~30%다.** 다만 이건 "코치가 광고를
          싣는 대가"이므로 광고 상품에 가깝고, 광고비는 보통 인앱결제 대상이
          아니다. 스토어 심사 전에 확인이 필요하다 — 여기서 틀리면 앱이
          리젝된다.
        · 약관에 광고 게재 조건·환불 규정을 적어야 한다 (A의 이용약관과 함께)

      **당분간은 계좌이체가 낫다.** 코치가 몇 명일 때 PG 수수료와 심사 시간을
      들일 이유가 없다. 지금 장부만으로도 누가 안 냈는지는 정확히 보인다.

- [ ] **코치 영상 저작권·검수 기준** — 승인이 곧 지급 결정이라 기준이 필요하다.
      지금은 유튜브 링크만 받는다(직접 업로드는 저장 비용과 검수 부담이 커서
      열지 않았다). 남의 영상을 자기 것처럼 올리는 경우를 어떻게 걸러낼지,
      승인 후 영상이 삭제되면 지급을 어떻게 할지 정해 두어야 한다.

---

## D-2. 언제 해야 하는가 — 스토어 등록 전 / 후

질문: "결제 같은 건 앱 등록하고 운영하다가 중간에 붙여도 되나?"
**대체로 그렇다.** 앱은 업데이트할 때마다 어차피 심사를 받는다. 기능을
나중에 붙이고 그 업데이트가 심사를 통과하면 그만이다. 새로 등록하는 것이
아니다. 아래만 예외다.

### 나중에 붙여도 되는 것 (업데이트 심사로 충분)
  · PG 계약 / 카드 결제 · 코치 구독 결제 · 드랍십 판매
  · 소셜 로그인 (다만 아래 서명 키 주의)
  · 카카오 알림톡 · 영수증 OCR · 고정형 가상계좌

### 나중에 하면 되돌릴 수 없거나 크게 번거로운 것
  1. **패키지명** `com.donghyun.tennismatch`
     한 번 출시하면 **영영 못 바꾼다.** 바꾸려면 완전히 다른 앱이 되고
     기존 설치·리뷰·순위가 전부 사라진다. 지금 이 이름으로 갈지 결정한다.

  2. **서명 키와 SHA-1 지문** ⚠️ 소셜 로그인의 함정
     Play 앱 서명을 쓰면 구글이 **업로드한 APK 를 자기 키로 다시 서명한다.**
     그래서 카카오/구글 로그인에 등록할 지문은 내 로컬 키가 아니라
     **Play 콘솔이 알려 주는 지문**이다. 이걸 모르면
     "개발 빌드에서는 로그인이 되는데 스토어에서 받은 앱은 안 되는" 현상이
     나고, 원인을 찾는 데 며칠이 간다.
     → 소셜 로그인을 나중에 붙이더라도 **두 지문을 다 등록**하면 된다.

  3. **데이터 안전 양식 (Play) / 앱 개인정보 (App Store)**
     최초 등록에 필수이고, **수집 항목이 늘 때마다 갱신**해야 한다.
     결제·배송지·결제수단을 추가하면 반드시 고쳐야 한다. 실제와 다르면
     경고 후 앱이 내려간다. "기능만 붙이고 양식은 안 고치는" 것이 가장
     흔한 사고다.

  4. **개인정보처리방침** — 최초 등록에 URL 이 없으면 등록 자체가 안 된다.
     나중에 수집 항목이 늘면 방침 문서도 같이 고친다.

  5. **계정 삭제 수단** — 최초 심사 항목이다. 나중이 아니라 지금.

### 결제에서 진짜 조심할 것 — 인앱결제 강제 여부
스토어가 자기 결제(수수료 15~30%)를 강제하는 기준은 대략
**"앱 안에서 쓰는 디지털 재화·구독인가"** 이다.
실물 상품, 앱 밖 오프라인 서비스, 광고 게재비는 통상 대상이 아니다.

우리 것에 대입하면
  · **회비** — 회원이 클럽에 내는 오프라인 활동비다. 앱은 송금 앱을
    열어 줄 뿐 돈을 받지 않는다. 대상 아님. **지금 방식이 안전하다.**
  · **코치 광고비** — 코치(사업자)가 광고를 싣는 대가. 광고 상품이므로
    통상 대상 아님. 단, 화면 문구를 "앱 기능 구독"처럼 쓰면 디지털
    구독으로 보일 수 있다. **"광고 게재"라는 성격을 문구에 유지**한다.
  · **용품 드랍십** — 실물 배송. 대상 아님.

⚠️ 다만 이 판단은 스토어 정책 문서를 직접 확인해야 한다. 정책은 바뀌고,
   같은 상품도 설명하기에 따라 분류가 달라진다. **결제를 실제로 붙이기
   전에 Play/App Store 의 최신 결제 정책을 읽고, 애매하면 심사팀에
   문의한다.** 여기서 틀리면 리젝되거나 최악의 경우 앱이 내려간다.
   (한국은 인앱결제 강제를 제한하는 법이 있지만 스토어의 실제 운영과
   맞물려 복잡하다 — 법이 있으니 괜찮겠지, 로 넘기지 않는다)

**결론**: 결제는 지금 미룬다. 대신 위 1·2번(패키지명·서명 키)은 첫 출시
전에 정해 두고, 3·4·5번은 첫 출시에 반드시 포함한다.

---

## E. 이미 처리된 것 — 다시 조사하지 않기 위해 남긴다

- [x] **C 항목 다섯 가지 — "조용히 터지는 것"들** (2026-08-21)

      다섯 개의 공통점은 고장 나도 오류가 안 뜬다는 것이었다. 그래서
      고치는 것보다 "잘라도 거짓말은 안 하게" 만드는 데 더 신경 썼다.

      **모임 구독 창 (`src/lib/meetingWindow.js`)**
      최근 12개월 + 미래만 실시간 구독한다. 주 2회 × 2년이면 200건이고
      앱을 켤 때마다 전부 읽었다.

      ⚠️ 여기서 제일 조심한 것: 그냥 자르면 재작년 랭킹이 조용히 0이 된다.
      화면에는 숫자가 멀쩡히 떠 있어서 아무도 잘렸다고 생각하지 않는다.
      그래서
        · 랭킹 화면은 안 불러왔을 때 "최근 12개월"이라고 적고
          [전체 불러오기]로 과거를 한 번만 읽어 합친다
        · 홈의 [내 기록]도 "최근 12개월 기준"이라고 적는다
        · 인수인계의 "쌓인 기록 · 모임 N"은 서버에 세어 달라고 한다
          (`getCountFromServer` — 문서를 안 읽어서 200건이든 2000건이든 같다).
          바로 위에 "총무가 바뀌어도 남습니다"라고 적혀 있어서 1년치를
          보여 주면 거짓말이 된다.
      합칠 때 id 로 덮어쓴다. 이어 붙이면 겹치는 모임이 두 번 세어져
      승수와 출석률이 부풀려진다.

      **채팅 채널 서버 필터**
      전체에서 최근 300개를 받아 화면에서 나누던 것을 채널 조건으로
      바꿨다. 코트장이 셋인데 한 채널에 대화가 몰리면 300개가 그 채널로
      다 차서 다른 채널은 최근 글까지 사라진다 — 오류 없이.
      복합 색인을 `firestore.indexes.json` 에 넣고 `firebase.json` 에
      연결했다. **색인을 배포하지 않으면 대화가 안 보인다**
      (`firebase deploy --only firestore:indexes`).
      채널 칩의 점은 채널당 1건씩 따로 물어본다 — 300개를 훑던 것보다 싸다.

      **교류전 초대 만료 (`clubMatch.isArchived` / `splitMatches`)**
      답 없는 초대가 "수락 대기"로 영원히 남아 목록 위를 차지했다.
      지우지는 않는다 — "초대했는데 답이 없었다"도 정보다. 아래로 접고
      왜 접혔는지 한 줄로 적는다. 서버에 상태를 바꿔 쓰지 않는다:
      날짜만 보면 아는 것을 매일 도는 작업으로 만들면 그 작업이 한 번
      실패할 때 목록이 다시 뒤엉킨다.

      **상대 클럽 선수 이름 (`rosterExpired` / `scrubRoster`)**
      교류전 문서는 루트(`clubMatches`)에 있어 두 클럽이 같이 본다.
      그래서 상대 클럽 회원의 이름·성별이 소속 클럽 밖에 남고, 상대가
      앱을 지워도 남는다. 개인정보처리방침에 **경기일로부터 1년**을
      적고, 운영진이 [클럽 교류전] → [지난 교류전]에서 직접 지울 수
      있게 했다. 이름만 "선수1"로 바꾸고 id·성별·경기 결과는 남긴다 —
      지난 승패까지 사라지면 두 클럽 모두 손해다.

      **코트 데이터 갱신 (`courtDataAge` / `courtReports`)**
      `scripts/fetch-courts.mjs` 가 받은 날짜를 파일에 같이 굽는다.
      180일이 지나면 코트 검색 화면이 "언제 받은 값"인지 적는다.
      최신이면 아무 말도 안 한다 — 멀쩡할 때 잔소리하면 다음 경고도
      안 읽는다. 그리고 수백 곳을 직접 확인할 수 없으니 [정보 신고]를
      붙였다. 신고에는 uid 가 들어가므로 목록은 앱 운영자만 읽는다
      (누가 어느 코트를 보려 했는지는 남이 알 일이 아니다).
      신고자 본인도 수정·삭제할 수 없다 — 나중에 바꾸면 기록의 뜻이 없어진다.

      규칙 267 → 277, 새 검사 `test:scale` 63건.

- [x] **회비·알림 하이어라키 + 일정 통합 + 대회 신청** (2026-08-21)

      **전체 ↔ 코트장 두 층** (`src/lib/scope.js` + 서버 사본 `functions/scope.js`)
      "코트장에 값이 있으면 그 값, 없으면 클럽 값". 회비(금액·납부일·계좌)와
      알림 7종이 같은 규칙을 쓴다.

      ⚠️ 회비가 조용히 두 배가 되지 않게 — 청구 단위(`feeScope`)는 클럽이
      명시적으로 고른다. 기본값은 예전과 같은 '클럽 하나로'다. 인헤리턴스
      (물려받음)와 별도 청구는 다른 이야기인데, 이걸 합치면 코트장 두 곳에
      나가는 회원이 하루아침에 두 배를 내게 된다.

      ⚠️ "껐다"와 "설정한 적 없다"는 다르다. `venue.notify.fee || club...` 로
      짜면 코트장에서 끈 알림이 클럽의 true 에 먹혀 "껐는데 계속 온다"가
      된다. 빈칸 저장도 값 저장이 아니라 **삭제**로 처리한다(0원 코트가 실재한다).

      문서 이름은 `feeDocKey(기간, 단위)`. 전체는 기간 그대로라 이미 쌓인
      `fees/{yyyy-mm}` 을 그대로 읽고 쓴다. 매일 도는 독촉도 청구 단위마다
      한 번씩 돌고, 발송 기록도 단위별로 남는다 — 염곡 1차를 보냈다고
      수도공고까지 보낸 것으로 기록되면 그 코트는 영영 안내를 못 받는다.
      `test-manager.mjs` 가 앱↔서버 사본을 29건 대조한다(일부러 어긋내
      실패하는 것을 확인했다).

      **일정 한 화면** (`src/lib/agenda.js`)
      우리 모임 · 대회 · 게스트 모집을 한 줄로 세우고 목록 ↔ 달력을
      전환한다. 달력은 직접 그린다(네이티브 모듈이 붙으면 OTA 로 못 고친다).
      빈칸도 칸으로 채운다 — null 로 두면 요일이 밀린다.
      코트장을 골라도 대회·게스트는 남는다. 사라지면 대회를 놓치고,
      대회는 신청 기간이 짧아 한 번 놓치면 끝이다.

      **대회 참가 신청**
      운영진이 모집을 열면(정원·마감일·참가비) 회원이 일정 카드에서
      바로 신청한다. 신청자는 대회 문서 안의 맵 — 하위 컬렉션이면 일정
      화면이 대회 수만큼 구독을 걸어야 한다. 규칙은 `applicants.{본인 uid}`
      만 허용하며, 규칙을 일부러 약화시켜 세 건이 실패하는 것을 확인했다.


- [x] **B 항목 정리 — 개발 잔재 걷어내고 인수인계 길 만들기** (2026-08-21)

      · **보기 모드 전환을 앱 운영자에게만** — 내가 두 화면을 번갈아 보려고
        넣은 개발 도구였다. 클럽 운영진이 보면 "내가 지금 운영진인가 회원인가"를
        헷갈리고, 모드를 바꿔 둔 채 잊으면 "메뉴가 사라졌다"고 하게 된다.

      · **데모 시드 기본값 true → false** — 지금 부르는 곳은 항상 false 를
        넘기므로 실제 사고는 없었다. 다만 나중에 인자를 빼고 부르면 그 순간
        남의 클럽에 모르는 이름이 생긴다. 한 글자짜리 방어.

      · **[앱 운영 관리] 화면 신설** (앱 운영자 전용) — 두 가지를 콘솔에서
        앱으로 옮겼다.
          공개 클럽 목록: 시험 삼아 만든 클럽의 **검색 노출을 여기서 끈다.**
            예전에는 클럽마다 계정을 갈아타야 했다. 클럽 자체는 지우지 않고
            검색에서만 뺀다 — 그 클럽 회원은 그대로 쓴다.
          앱 운영자 명단: **앱 운영자가 다음 사람을 세울 수 있다.** 예전에는
            콘솔에서만 가능해서, 내가 사라지면 아무도 용품·코치를 관리할 수
            없었다. 처음 한 명만 콘솔에서 만든다(그래야 시작한다).
            **스스로는 못 내린다** — 마지막 한 명이 자기를 내리면 아무도 안
            남아서 콘솔로만 되돌릴 수 있다. 규칙에서 막는다.

      **검증**: 규칙 테스트 258건(9건 추가). 일반 회원이 스스로 앱 운영자가
      되는 것, 클럽 회장이 앱 운영자를 세우는 것, 스스로를 내리는 것,
      무관한 회원이 남의 클럽 노출을 바꾸는 것이 모두 막히는 것을 확인했다.

      ⚠️ 이 작업 중에 옛 테스트 하나가 정책 변경과 충돌했다. "앱 운영자도
      명단 추가 거부"가 이제 성공하면서 **실제로 `appAdmins/mem1` 을 만들어
      버렸고**, 그 뒤 "회원은 광고 성과를 못 본다" 테스트 두 개가 무너졌다.
      정책을 바꿀 때는 그 정책을 검사하던 옛 테스트도 같이 고쳐야 한다.
      (승격 대상은 아무 데도 안 쓰는 uid 로 바꿨다)

- [x] **국외 이전 조항을 사실대로 고침** (2026-08-21)
      처음 초안에는 Firebase 를 **국외 이전**으로 적어 두었다. 리전을 확인해
      보니 `asia-northeast3` — **서울**이었다. 회원 정보와 클럽 활동 기록은
      국내에 있다. 사실과 다른 고지였다.

      실제로 국외로 나가는 것은 알림을 중계하는 **Expo(미국)로 가는 푸시 토큰**
      하나뿐이다. 그래서 5항을 "처리 위탁"으로 되돌리고, 5-1 항에 국외 이전을
      따로 떼어 이전 항목·받는 자·국가·시점·목적·보유기간을 적었다.
      거부 방법도 적었다 — 휴대폰에서 알림을 끄면 되고, 다른 기능은 그대로 쓴다.

      **교훈**: 서버 함수가 도는 리전(asia-northeast3)을 데이터 보관 위치로
      넘겨짚을 뻔했다. 둘은 별개다. 법적 고지에 들어가는 값은 짐작하지 않는다.

- [x] **계정 삭제** (2026-08-21)
      [더보기] → [약관·개인정보] → [계정 삭제]. 스토어 심사 항목이었다.

      **무엇을 지우고 무엇을 남기는가**
        지운다: 로그인 계정, `users/{uid}`(소속 클럽·프로필), 연락처, 푸시토큰
        남긴다: **지난 대진표·정산에 남은 이름**. 이름까지 지우면 함께 뛴
        사람들의 기록이 "(탈퇴)"만 남아 읽을 수 없게 된다. 그래서 회원 문서를
        지우지 않고 **비워서** 남긴다(이름·성별·조만 남기고 나머지는 삭제).

      **재가입하면**: Firebase 계정을 지우면 다음 가입 때 새 uid 가 나온다.
      회원 문서는 옛 uid 로 키가 잡혀 있어 이어지지 않는다 — "예전 클럽과
      무관하게 새로 시작"이 저절로 된다.

      **회장이 나가면**: 운영진 **전원**이 회장이 된다. 한 명만 고르면
      "왜 저 사람이냐"가 생기고, 아무도 안 올리면 역할을 바꿀 사람이 없어
      클럽이 통째로 잠긴다. 운영진이 없으면 남은 회원 전원, 아무도 없으면
      빈 클럽으로 닫고 검색에서 뺀다.

      **순서**: 본인 확인 → 클럽 정리 → 개인정보 삭제 → 계정 삭제.
      본인 확인을 맨 앞에 둔 이유는, 마지막에 `requires-recent-login` 으로
      막히면 **데이터만 지워지고 계정은 남는** 최악의 상태가 되기 때문이다.

      실수 방지: "계정 삭제" 를 정확히 입력해야 버튼이 열린다(띄어쓰기까지).

      **검증**: `test-delete.mjs` 53건. 이름이 남는 것, 연락 수단이 하나도
      안 남는 것, 겸임 회장을 알아보는 것, 이미 탈퇴한 사람에게 안 넘기는 것,
      마지막 한 명이면 빈 클럽이 되는 것을 각각 확인했다.

- [x] **푸시 알림 — 끝까지 확인함 (토큰 → 서버 → 기기 도착)** (2026-08-21)
      알림이 통째로 죽어 있던 것을 살렸다. 원인은 두 겹이었다.

      **원인 1 — 안드로이드 FCM 미설정 (진짜 원인)**
        이 앱은 Firebase **웹** SDK 로 데이터만 다루는데, 그건 네이티브
        FirebaseApp 을 초기화하지 않는다. 안드로이드 푸시는 네이티브 FCM 을
        쓰므로 `google-services.json` 이 빌드에 들어가야 토큰이 나온다.
        해결: Firebase 콘솔에서 Android 앱 추가 → `google-services.json` 을
        루트에 두고 `app.json` 의 `android.googleServicesFile` 로 가리킴 →
        `eas credentials` 에서 **Google Service Account** → FCM V1 키 업로드
        → APK 재빌드.
        ⚠️ EAS 메뉴에서 이름이 `Push Notifications` 가 아니라
        `Google Service Account` 다. `Push Notifications (Legacy)` 는 다른 것이다.

      **원인 2 — 콘솔에서 엉뚱한 클럽을 보고 있었다 (이틀을 여기서 썼다)**
        토큰은 `clubs/{지금 들어와 있는 클럽}/members/{uid}` 에만 저장된다.
        테스트하며 만든 클럽이 5개였고, 앱은 `ZSq9K4Kl…` 에 들어가 있는데
        콘솔에서는 `FRxeeWj…`(써티포티)를 열어 놓고 "토큰이 없다"고 했다.
        게다가 콘솔은 필드를 **알파벳 순**으로 정렬해서 `pushToken` 이
        `name` 과 `role` 사이에 끼어 들어간다 — 맨 아래를 보면 못 찾는다.

      **다시 안 헤매게 만든 것**
        · `registerPushToken` 이 `{ ok, reason, detail, token }` 을 돌려준다.
          막히는 자리를 넷으로 나눴다: 권한 / projectId / 토큰 발급 / 저장.
        · [더보기] → **알림 상태** 화면. 권한·토큰·실패 사유를 그대로 보여 주고,
          **토큰이 저장된 전체 경로**를 눌러서 복사할 수 있다.
        · **[나에게 테스트 알림]** — 서버가 그 회원 문서에서 토큰을 직접 읽어
          본인에게 한 건 보내고, 무엇을 봤는지 결과에 적는다. 앱 문제인지
          서버 문제인지 Expo 문제인지 한 번에 갈린다.
          (남에게 쏘는 통로가 되지 않도록 `by` 가 본인이어야 하고 결과는
           클라이언트가 못 고친다. 규칙 테스트 5건으로 막아 두었다)

      **검증**: 앱에서 `받아서 저장됨` + 저장 경로 확인 → [나에게 테스트 알림]
      → `서버가 보냈습니다` → **실제 기기에 푸시 도착**. 끝까지 확인했다.

      **남은 정리**: 테스트로 만든 클럽 5개. 그대로 두면 클럽 검색에 뜨고,
      앞으로도 "어느 클럽을 보고 있는가"로 헷갈린다. (아래 B 항목)

- [x] **함수 배포가 "Timeout after 10000" 으로 막히던 것 — 로컬 Node 버전** (2026-08-21)
      `firebase deploy --only functions` 가 이 오류로 죽었다.

      ```
      Error: User code failed to load. Cannot determine backend specification.
      Timeout after 10000.
      ```

      **코드 문제가 아니다.** 같은 코드를 리눅스에서 `require` 하면 278ms 에
      함수 15개가 정상 로드된다. 배포 전 "탐색" 단계에서 CLI 가 로컬 Node 로
      함수를 띄우고 localhost 로 함수 목록을 물어보는데, 그 응답을 10초 안에
      못 받은 것이다.

      원인은 **로컬 Node 24 vs 선언된 Node 22**. Node 17 부터 `localhost` 가
      IPv6(::1) 로 먼저 해석되는데 서버는 IPv4 에 붙어서 서로 못 만난다.
      Windows 에서 특히 잘 난다.

      **당장 푸는 법** (그 창에서만 유효)
      ```
      set NODE_OPTIONS=--dns-result-order=ipv4first
      firebase deploy --only functions
      ```

      **제대로 푸는 법**: 로컬 Node 를 22 로 맞춘다(nvm-windows).
      런타임과 같은 버전을 쓰면 이 부류가 다시 안 생긴다.
      맞춘 뒤에는 `functions/node_modules` 를 지우고 다시 설치한다.

      ⚠️ `functions/package.json` 의 `engines.node` 는 **배포될 런타임**이다.
      로컬에 맞추려고 이 값을 24 로 올리면 안 된다 — 서버 런타임이 바뀐다.

      (2026-08-18 에도 같은 오류를 만났는데 그때는 firebase-functions 5 → 7
      업그레이드로 풀렸다. 증상이 같아도 원인이 다를 수 있다는 뜻이다.
      먼저 `cd functions && node -e "require('./index.js')"` 로 코드부터
      가려내면 헛다리를 짚지 않는다.)

- [x] **코치 · 코트 정보 · 간편송금 · 용품 드랍십 뼈대** (2026-08-19)
      한 번에 들어온 요구 여섯 가지 중 앱 안에서 끝나는 것을 만들었다.

      · **코치 영상 승인** — 코치가 유튜브 링크로 올리고, 앱 주인이 승인해야
        회원에게 보인다. 승인이 곧 "이번 달 지급"이므로 승인한 사람과 시각을
        남긴다. 반려는 사유 없이는 안 된다 — 코치가 무엇을 고칠지 모른 채
        같은 것을 다시 내는 왕복을 막기 위해서다.
      · **코치 검색** — 시/도 → 시·군·구. 사는 곳뿐 아니라 **가르치는 코트의
        지역으로도** 걸린다. 강남 사는 코치가 송파에서 가르치면 송파 검색에
        나온다. 이걸 안 하면 출강 코치가 검색에서 통째로 사라진다.
      · **코치 프로필** — 경력·자격증·레슨 코트·레슨 시간·레슨비 안내.
        예약·결제·코치 관리는 요구대로 넣지 않았다.
      · **광고비 청구** — 그 달에 **광고가 실제로 나간**(승인된 영상이 있는)
        코치만 청구 대상이 된다. 노출이 없으면 받을 근거도 없다. 금액은
        3~5만원 범위를 벗어나면 화면이 막고, 문서 id 를 `코치_년월`로 계산해
        같은 달에 두 번 청구되는 것을 구조적으로 막았다.
        ※ 처음에는 이 돈의 방향을 **거꾸로** 만들었다(앱이 코치에게 지급).
          2026-08-19 같은 날 바로잡았다. 코치가 앱에 내는 것이 맞다.
      · **코트 검색 → 클럽·코치** — 코트 카드에서 [클럽 2 · 코치 3]을 누르면
        그 코트를 쓰는 클럽, 레슨하는 코치, 요일별 레슨 시간이 펼쳐진다.
        코트는 Firestore 문서가 아니라 앱 내장 표라서 문서 id 가 없다. 그래서
        "시/도 + 정규화한 이름"을 키로 쓴다 — "올림픽공원 테니스장",
        "올림픽공원테니스장", "올림픽공원 코트"가 한 곳으로 모인다.
      · **간편송금** — 위 D 참고. 계약 없이 이미 동작한다.
      · **용품 드랍십** — 등록 화면과 마진 계산까지. 실제 판매는 D 참고.

      **검증**: 새 테스트 198건(`test-coach.mjs`) + 보안 규칙 테스트 34건 추가.
      규칙 테스트에서 코치가 스스로 승인 상태로 올리는 것, 남의 이름으로 영상을
      올리는 것, 승인된 내용을 승인 상태 그대로 바꿔치우는 것, 코치가 자기
      지급액을 고치는 것, 남의 주문(배송지)을 읽는 것이 전부 막히는 것을 확인했다.

- [x] **대진 탭이 열자마자 죽던 문제 — 선언보다 먼저 읽은 값** (2026-08-19)
      대진 ↔ 참석 어긋남 검사를 붙이면서 `drawDiff` 의 `useMemo` 가
      `matches` 를 읽게 되었는데, `matches` 는 그보다 100줄 아래 "화면"
      구역에서 선언돼 있었다. `const` 는 선언 줄보다 먼저 읽으면 예외를
      던진다. 문법은 멀쩡해서 번들 빌드도 임포트 검사도 통과했고, 탭을
      실제로 눌러야만 터졌다.
      `matches` 선언을 위로 올려 고쳤다.
      **다시 안 생기게**: `scripts/test-imports.mjs` 에 세 번째 검사를
      넣었다. 훅의 의존성 배열에 적힌 이름이 같은 함수 본문에서 더
      아래에 선언되었으면 잡는다. 의존성 배열은 화면을 그릴 때마다 반드시
      평가되므로 헛경보가 없다. 안쪽 함수의 지역 변수(`gen()` 안의 다른
      `matches`)와 다른 컴포넌트의 같은 이름은 중괄호 깊이와 함수 구역으로
      갈라 낸다 — 이 두 가지를 안 갈랐을 때 실제로 헛경보와 놓침이 각각
      한 번씩 났다.
      **검증**: 고치기 전 파일(`git show HEAD:app/(tabs)/match.jsx`)을
      그대로 넣어 `match.jsx:340 — 'matches' 을 선언(437줄)보다 먼저
      씁니다` 로 잡히는 것을, 고친 파일에서는 0건인 것을 확인했다.

- [x] **회비 독촉 앱↔서버 대조** (2026-08-18)
      가장 먼저 갚아야 할 빚으로 적어 두었던 것. 서버가 손으로 적은
      사본을 쓰고 있었고, 실제로 어긋나 있었다 — 앱은 최종 단계에
      "사정이 있으시면 운영진에게 알려 주세요"를 쓰는데 서버는 2차 문구를
      그대로 다시 보냈다. `functions/dunning.js` 로 사본을 정식화하고
      `test-manager.mjs` 가 48건을 대조한다. 서버의 발송 함수는 이제
      Firestore 읽기·쓰기만 하고 판단은 하지 않는다.

- [x] **죽은 푸시 토큰 정리** (2026-08-18)
      Expo 응답을 읽지 않아 만료 토큰(`DeviceNotRegistered`)이 계속 쌓였다.
      "20명에게 보냈다"는데 실제로 받는 사람은 12명인 상태가 되고 발송
      통계가 거짓말을 시작한다. 응답을 토큰별로 짚어 죽은 것은 회원
      문서에서 지운다.

- [x] **Cloud Functions 런타임 Node 22** (2026-08-18)
      Node 20 이 2026-10-30 에 내려가므로 미리 올렸다. firebase-admin 도
      14 로 함께 올렸다(14 는 Node 22 를 요구해서 런타임과 짝이다).
      함수 15개가 모두 로드되는 것을 확인했다.

- [x] **참석 투표 로직의 앱↔서버 대조** (2026-08-18)
      `src/lib/rsvpAsk.js` 와 `functions/rsvpAsk.js` 가 두 벌 존재하는 것은
      배포 구조상 어쩔 수 없다. 대신 `scripts/test-rsvp.mjs` 가 두 구현에
      같은 입력 54건을 넣어 답이 같은지 매번 대조한다. 한쪽만 고치면
      테스트가 깨진다. (서버 기본값을 3일→2일로 바꿔 7건이 잡히는 것을
      실제로 확인함)

- [x] **교류전 권한 경계** (2026-08-18)
      두 클럽이 같은 문서를 본다. 대진·결과·설정은 개설한 클럽만,
      초대받은 클럽은 수락·거절과 자기 명단만 쓴다. 상대가 우리 명단을
      고칠 수 있으면 사고이므로 보안 규칙에서 막고 22개 케이스로 검증한다.

- [x] **교류전 문구의 앱↔서버 대조** (2026-08-18)
      `src/lib/clubMatch.js` ↔ `functions/clubMatch.js`, 24건 대조.
      서버는 알림 문구에 필요한 것만 베낀다 — 안 쓰는 것까지 베끼면
      어긋날 표면만 넓어진다.

- [x] **곁가지 키가 배포를 막지 않게** (2026-08-18)
      날씨 함수가 `defineSecret('KMA_SERVICE_KEY')` 를 요구해서, 그 시크릿이
      없으면 배포가 통째로 실패했다. 쓰지도 않는 키 하나 때문에 참석 알림·
      교류전 푸시까지 못 올라갔다. `process.env` 로 바꿔 키가 없으면 그
      함수만 쉰다. 앞으로 외부 키가 필요한 함수도 같은 원칙을 지킨다.

- [x] **화면 이름 어긋남 방지** — 안내 문구의 `[회원]` 과 메뉴의
      `[회원 목록]` 이 달라 못 찾던 문제. 이름을 `constants.js` 의 `SCREEN`
      한 곳에서만 정하고 `scripts/test-labels.mjs` 가 어긋남을 잡는다.

- [x] **import 누락으로 인한 강제 종료** — 결산 화면이 죽던 원인.
      `scripts/test-imports.mjs` 가 없는 export·안 가져온 사용을 1,095건
      검사한다(현재 1,156건).

- [x] **회비·미납 정보 노출 범위** — 회원은 서로의 납부 여부를 볼 수 없다.
      보안 규칙에서 `fees` 를 회장·총무로 제한하고 테스트로 검증한다.

- [x] **참석 투표 요청 권한** — 아무나 전체 알림을 쏘는 통로가 되지 않게
      `pushJobs` 생성은 운영진만, 발송 결과는 서버만 쓴다.

---

## 갱신 규칙

기능을 추가하거나 요구사항을 반영할 때마다:

1. 그 기능이 **지금은 괜찮지만 남이 쓰면 곤란한 부분**을 A~D 중 맞는 곳에 적는다.
2. 빚을 갚았으면 E로 옮기고 **날짜와 검증 방법**을 같이 남긴다.
3. 맨 위 "마지막 갱신" 날짜를 고친다.

적을 것이 없다고 판단했으면 그것도 괜찮다. 다만 "없다"고 확인한 것과
"안 봤다"는 다르므로, 애매하면 적어 두는 쪽을 택한다.
